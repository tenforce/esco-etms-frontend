{{#if (is-pending members)}}
  <i class="fa fa-circle-o-notch fa-spin"></i>
{{else}}
  {{#power-select options=members disabled=disabledSelect searchEnabled=false selected=selectedLevel onchange=(action "selectLevel") as |member|}}
    {{await member.localizedPrefLabel}}
  {{/power-select}}

  {{#if showActions}}
    {{#if model.skillReuseLevelSaving}}
      <div class="actions">
        <i class="fa fa-circle-o-notch fa-spin"></i>
      </div>
    {{else if dirty}}
      <div class="actions">
        <button type="button" tabindex=-1 class="save" title="Save" {{action 'saveSkillReuseLevel'}} disabled={{disabled}}></button>
        <button type="button" tabindex=-1 class="cancel" title="Reset" {{action 'rollbackSkillReuseLevel'}} disabled={{disabled}}></button>
      </div>
    {{/if}}
  {{/if}}
{{/if}}
