`import { dialogTargetGenerator } from '../../../helpers/dialog-target-generator'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | dialog target generator'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = dialogTargetGenerator 42
  assert.ok result
